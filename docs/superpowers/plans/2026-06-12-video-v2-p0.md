# 影视区 V2 — P0 实施计划（核心层 + MacCMS + UI）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完全重写影视区，建立 MediaSource 抽象层、适配 MacCMS 源、重写 UI 为 Bangumi 元数据中心模式

**Architecture:** 参考 Animeko 的 MediaSource/MediaSourceFactory 模式，建立 TypeScript 接口体系。搜索流程从"关键词搜视频"变为"先搜 Bangumi 条目 → 选集 → 按剧集查资源"。UI 分为影视首页（Bangumi 搜索）、番剧详情页（条目信息+剧集+资源）、播放页（DPlayer+弹幕）。

**Tech Stack:** React 18, Vite, DPlayer, Hls.js, Cloudflare Workers, Bangumi API

---

## File Structure

```
src/
├── services/
│   └── media/
│       ├── types.ts                # 所有接口/类型定义
│       ├── MediaSourceManager.ts   # 源注册/发现/调度
│       ├── MediaSelector.ts        # 资源选择/排序/过滤
│       ├── MatchEngine.ts          # 匹配引擎
│       └── sources/
│           └── MacCMSSource.ts     # 苹果CMS 源
├── components/
│   └── Video/
│       ├── VideoHome.jsx           # 影视首页（替换 VideoZone）
│       ├── VideoHome.css
│       ├── SubjectDetail.jsx       # 番剧详情页（替换 VideoDetail）
│       ├── SubjectDetail.css
│       ├── VideoPlayer.jsx         # 播放器页（新增）
│       ├── VideoPlayer.css
│       ├── SourceManager.jsx       # 源管理页（新增）
│       ├── SourceManager.css
│       ├── MediaMatchList.jsx      # 资源匹配列表组件
│       └── MediaMatchList.css
```

**删除旧文件：**
- `src/components/Video/VideoZone.jsx`
- `src/components/Video/VideoZone.css`
- `src/components/Video/VideoDetail.jsx`
- `src/components/Video/VideoDetail.css`
- `src/services/videoSource.js`

---

### Task 1: 核心类型定义

**Files:**
- Create: `src/services/media/types.ts`

- [ ] **Step 1: 创建 types.ts，定义所有核心接口**

```typescript
// src/services/media/types.ts

export enum MediaSourceKind {
  WEB = 'web',
  BITTORRENT = 'bittorrent',
  LOCAL_CACHE = 'local_cache',
}

export enum MatchKind {
  EXACT = 'exact',
  FUZZY = 'fuzzy',
}

export enum ConnectionStatus {
  AVAILABLE = 'available',
  UNAVAILABLE = 'unavailable',
  TIMEOUT = 'timeout',
}

export interface MediaSourceInfo {
  displayName: string;
  description?: string;
  websiteUrl?: string;
  iconUrl?: string;
  isSpecial?: boolean;
  tier?: number;
}

export interface MediaSource {
  readonly sourceId: string;
  readonly kind: MediaSourceKind;
  readonly info: MediaSourceInfo;
  checkConnection(): Promise<ConnectionStatus>;
  fetch(request: MediaFetchRequest): Promise<PagedResult<MediaMatch>>;
  close?(): void;
}

export interface MediaSourceFactory {
  readonly factoryId: string;
  readonly allowMultipleInstances: boolean;
  readonly parameters: SourceParameter[];
  readonly info: MediaSourceInfo;
  create(sourceId: string, config: SourceConfig): MediaSource;
}

export interface MediaFetchRequest {
  subjectId: string;
  episodeId?: string;
  subjectNames: string[];
  episodeSort: string;
  episodeName?: string;
}

export interface MediaMatch {
  media: Media;
  matchKind: MatchKind;
}

export interface Media {
  mediaId: string;
  sourceId: string;
  title: string;
  episodeRange?: EpisodeRange;
  download?: MediaDownload;
  properties: Record<string, any>;
}

export interface EpisodeRange {
  sort: string;
  ep?: string;
  name?: string;
}

export interface MediaDownload {
  kind: 'http' | 'magnet' | 'torrent' | 'local';
  url: string;
  headers?: Record<string, string>;
}

export interface PagedResult<T> {
  items: T[];
  total: number;
  page: number;
  pagecount: number;
  hasMore: boolean;
}

export interface SourceConfig {
  arguments: Record<string, string | undefined>;
  serializedArguments?: any;
}

export interface SourceParameter {
  name: string;
  displayName: string;
  type: 'string' | 'number' | 'boolean' | 'select';
  default?: any;
  options?: { label: string; value: string }[];
  required?: boolean;
}
```

- [ ] **Step 2: 验证类型文件无语法错误**

Run: `cd d:\Desktop\Ideas\ANISpace\ANISpace && npx tsc --noEmit src/services/media/types.ts 2>&1 || echo "Type check done"`

- [ ] **Step 3: Commit**

```bash
git add src/services/media/types.ts
git commit -m "feat(video-v2): add core type definitions for MediaSource system"
```

---

### Task 2: 匹配引擎

**Files:**
- Create: `src/services/media/MatchEngine.ts`

- [ ] **Step 1: 创建 MatchEngine.ts**

```typescript
// src/services/media/MatchEngine.ts
import { MatchKind, MediaFetchRequest, MediaMatch, Media } from './types';

export class MatchEngine {
  /**
   * 判断一个资源标题是否匹配请求中的番剧名称
   */
  static matchSubject(title: string, request: MediaFetchRequest): boolean {
    const lowerTitle = title.toLowerCase();
    return request.subjectNames.some(name => 
      lowerTitle.includes(name.toLowerCase())
    );
  }

  /**
   * 判断集数是否精确匹配
   */
  static matchEpisode(title: string, episodeSort: string): boolean {
    const sort = episodeSort.padStart(2, '0');
    const patterns = [
      new RegExp(`[^0-9]${sort}[^0-9]`),      // " 01 "
      new RegExp(`第${sort}集`),                // "第01集"
      new RegExp(`第${parseInt(sort)}集`),       // "第1集"
      new RegExp(`EP${sort}`, 'i'),             // "EP01"
      new RegExp(`\\b${sort}\\b`),              // standalone number
      new RegExp(`\\b${parseInt(sort)}\\b`),     // standalone number without padding
    ];
    return patterns.some(p => p.test(title));
  }

  /**
   * 为资源计算匹配度
   */
  static computeMatchKind(
    title: string,
    request: MediaFetchRequest,
    episodeRange?: { sort: string },
  ): MatchKind | null {
    // 先检查番剧名匹配
    if (!this.matchSubject(title, request)) {
      return null; // 完全不匹配
    }

    // 如果有 episodeId 精确匹配
    if (request.episodeId && episodeRange?.sort === request.episodeSort) {
      return MatchKind.EXACT;
    }

    // 检查集数匹配
    if (request.episodeSort && this.matchEpisode(title, request.episodeSort)) {
      return MatchKind.EXACT;
    }

    // 番剧名匹配但集数不精确
    return MatchKind.FUZZY;
  }

  /**
   * 对匹配结果排序：EXACT 优先，然后按 tier 排序
   */
  static sortMatches(matches: MediaMatch[]): MediaMatch[] {
    return [...matches].sort((a, b) => {
      // EXACT 优先
      if (a.matchKind !== b.matchKind) {
        return a.matchKind === MatchKind.EXACT ? -1 : 1;
      }
      // 同匹配度按 tier 排序（tier 越小越优先）
      const tierA = a.media.properties?.tier ?? 999;
      const tierB = b.media.properties?.tier ?? 999;
      return tierA - tierB;
    });
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/services/media/MatchEngine.ts
git commit -m "feat(video-v2): add MatchEngine for EXACT/FUZZY matching"
```

---

### Task 3: MediaSourceManager

**Files:**
- Create: `src/services/media/MediaSourceManager.ts`

- [ ] **Step 1: 创建 MediaSourceManager.ts**

```typescript
// src/services/media/MediaSourceManager.ts
import {
  MediaSource,
  MediaSourceFactory,
  MediaFetchRequest,
  MediaMatch,
  PagedResult,
  ConnectionStatus,
  SourceConfig,
} from './types';

const SOURCES_STORAGE_KEY = 'acg_v2_sources';
const DISABLED_KEY = 'acg_v2_sources_disabled';

interface SourceRegistration {
  sourceId: string;
  factoryId: string;
  config: SourceConfig;
  enabled: boolean;
  order: number;
}

class MediaSourceManager {
  private factories = new Map<string, MediaSourceFactory>();
  private instances = new Map<string, MediaSource>();

  /** 注册一个工厂 */
  registerFactory(factory: MediaSourceFactory): void {
    this.factories.set(factory.factoryId, factory);
  }

  /** 获取所有已注册的工厂 */
  getFactories(): MediaSourceFactory[] {
    return Array.from(this.factories.values());
  }

  /** 获取工厂 */
  getFactory(factoryId: string): MediaSourceFactory | undefined {
    return this.factories.get(factoryId);
  }

  /** 获取或创建源实例 */
  getSource(sourceId: string): MediaSource | undefined {
    if (this.instances.has(sourceId)) {
      return this.instances.get(sourceId)!;
    }
    const reg = this.getRegistrations().find(r => r.sourceId === sourceId);
    if (!reg) return undefined;
    const factory = this.factories.get(reg.factoryId);
    if (!factory) return undefined;
    const source = factory.create(reg.sourceId, reg.config);
    this.instances.set(reg.sourceId, source);
    return source;
  }

  /** 获取所有已启用的源实例 */
  getEnabledSources(): MediaSource[] {
    const regs = this.getRegistrations();
    const disabled = this.getDisabledIds();
    return regs
      .filter(r => r.enabled && !disabled.includes(r.sourceId))
      .sort((a, b) => a.order - b.order)
      .map(r => this.getSource(r.sourceId))
      .filter((s): s is MediaSource => s !== undefined);
  }

  /** 并行查询所有已启用源 */
  async fetchAll(request: MediaFetchRequest): Promise<{
    results: MediaMatch[];
    errors: { sourceId: string; error: string }[];
  }> {
    const sources = this.getEnabledSources();
    const promises = sources.map(async (source) => {
      try {
        const result = await source.fetch(request);
        return { sourceId: source.sourceId, matches: result.items };
      } catch (err: any) {
        return { sourceId: source.sourceId, error: err.message || '查询失败' };
      }
    });

    const settled = await Promise.allSettled(promises);
    const results: MediaMatch[] = [];
    const errors: { sourceId: string; error: string }[] = [];

    for (const r of settled) {
      if (r.status === 'fulfilled') {
        if ('error' in r.value && r.value.error) {
          errors.push({ sourceId: r.value.sourceId, error: r.value.error });
        } else {
          results.push(...(r.value as any).matches);
        }
      } else {
        errors.push({ sourceId: 'unknown', error: r.reason?.message || '未知错误' });
      }
    }

    return { results, errors };
  }

  /** 注册一个源（持久化到 localStorage） */
  addRegistration(reg: Omit<SourceRegistration, 'order'>): void {
    const regs = this.getRegistrations();
    regs.push({ ...reg, order: regs.length });
    this.saveRegistrations(regs);
  }

  /** 移除一个源 */
  removeRegistration(sourceId: string): void {
    const regs = this.getRegistrations().filter(r => r.sourceId !== sourceId);
    this.saveRegistrations(regs);
    const instance = this.instances.get(sourceId);
    if (instance) {
      instance.close?.();
      this.instances.delete(sourceId);
    }
  }

  /** 切换源启用/禁用 */
  toggleSource(sourceId: string): void {
    const disabled = this.getDisabledIds();
    const idx = disabled.indexOf(sourceId);
    if (idx >= 0) disabled.splice(idx, 1);
    else disabled.push(sourceId);
    localStorage.setItem(DISABLED_KEY, JSON.stringify(disabled));
  }

  /** 检查源是否禁用 */
  isDisabled(sourceId: string): boolean {
    return this.getDisabledIds().includes(sourceId);
  }

  /** 获取所有注册信息 */
  getRegistrations(): SourceRegistration[] {
    return JSON.parse(localStorage.getItem(SOURCES_STORAGE_KEY) || '[]');
  }

  private saveRegistrations(regs: SourceRegistration[]): void {
    localStorage.setItem(SOURCES_STORAGE_KEY, JSON.stringify(regs));
  }

  private getDisabledIds(): string[] {
    return JSON.parse(localStorage.getItem(DISABLED_KEY) || '[]');
  }

  /** 关闭所有源实例 */
  closeAll(): void {
    for (const instance of this.instances.values()) {
      instance.close?.();
    }
    this.instances.clear();
  }
}

// 全局单例
export const mediaSourceManager = new MediaSourceManager();
```

- [ ] **Step 2: Commit**

```bash
git add src/services/media/MediaSourceManager.ts
git commit -m "feat(video-v2): add MediaSourceManager for source lifecycle"
```

---

### Task 4: MacCMSSource 实现

**Files:**
- Create: `src/services/media/sources/MacCMSSource.ts`

- [ ] **Step 1: 创建 MacCMSSource.ts**

```typescript
// src/services/media/sources/MacCMSSource.ts
import {
  MediaSource,
  MediaSourceFactory,
  MediaSourceKind,
  MediaSourceInfo,
  MediaFetchRequest,
  MediaMatch,
  Media,
  MediaDownload,
  PagedResult,
  ConnectionStatus,
  SourceConfig,
  SourceParameter,
  MatchKind,
} from '../types';
import { MatchEngine } from '../MatchEngine';
import oauthConfig from '../../../../oauth.config.js';

const MACCMS_FACTORY_ID = 'maccms';

class MacCMSSource implements MediaSource {
  readonly sourceId: string;
  readonly kind = MediaSourceKind.WEB;
  readonly info: MediaSourceInfo;
  private baseUrl: string;
  private proxyBase: string;

  constructor(sourceId: string, name: string, baseUrl: string) {
    this.sourceId = sourceId;
    this.baseUrl = baseUrl.replace(/\/$/, '');
    this.proxyBase = oauthConfig.proxyUrl || '';
    this.info = {
      displayName: name,
      description: `苹果CMS源: ${name}`,
      websiteUrl: this.baseUrl,
      tier: 2,
    };
  }

  async checkConnection(): Promise<ConnectionStatus> {
    try {
      const url = this.buildUrl('/api.php/provide/vod/', { ac: 'list' });
      const res = await fetch(url, { signal: AbortSignal.timeout(5000) });
      return res.ok ? ConnectionStatus.AVAILABLE : ConnectionStatus.UNAVAILABLE;
    } catch {
      return ConnectionStatus.TIMEOUT;
    }
  }

  async fetch(request: MediaFetchRequest): Promise<PagedResult<MediaMatch>> {
    const keyword = request.subjectNames[0] || '';
    const url = this.buildUrl('/api.php/provide/vod/', { ac: 'videolist', wd: keyword });
    const res = await fetch(url);
    const data = await res.json();

    if (data.code !== 200 || !data.list) {
      return { items: [], total: 0, page: 1, pagecount: 0, hasMore: false };
    }

    const matches: MediaMatch[] = [];
    for (const item of data.list) {
      const matchKind = MatchEngine.computeMatchKind(item.vod_name, request);
      if (matchKind === null) continue;

      // 解析剧集
      const playFroms = (item.vod_play_from || '').split('$$$').filter(Boolean);
      const playUrlGroups = (item.vod_play_url || '').split('$$$').filter(Boolean);

      const episodes = playFroms.map((from, idx) => {
        const urlGroup = playUrlGroups[idx] || '';
        const eps = urlGroup.split('#').filter(Boolean).map(ep => {
          const parts = ep.split('$');
          return { name: parts[0] || `第${idx + 1}集`, url: parts[1] || parts[0] };
        });
        return { source: from, episodes: eps };
      });

      // 为每集创建 Media
      for (const group of episodes) {
        for (const ep of group.episodes) {
          const epMatch = MatchEngine.matchEpisode(ep.name, request.episodeSort);
          const finalKind = epMatch ? MatchKind.EXACT : matchKind;

          const media: Media = {
            mediaId: `${this.sourceId}_${item.vod_id}_${ep.name}`,
            sourceId: this.sourceId,
            title: `${item.vod_name} - ${ep.name}`,
            episodeRange: { sort: ep.name.replace(/[^0-9]/g, '') || ep.name, name: ep.name },
            download: {
              kind: 'http',
              url: this.buildStreamUrl(ep.url),
            },
            properties: {
              vodId: item.vod_id,
              cover: item.vod_pic,
              category: item.vod_class,
              year: item.vod_year,
              area: item.vod_area,
              remarks: item.vod_remarks,
              description: item.vod_content?.replace(/<[^>]+>/g, '') || '',
              playSource: group.source,
              tier: this.info.tier,
            },
          };

          matches.push({ media, matchKind: finalKind });
        }
      }
    }

    return {
      items: MatchEngine.sortMatches(matches),
      total: data.total || matches.length,
      page: data.page || 1,
      pagecount: data.pagecount || 1,
      hasMore: (data.page || 1) < (data.pagecount || 1),
    };
  }

  private buildUrl(path: string, params: Record<string, string>): string {
    const query = new URLSearchParams(params).toString();
    if (this.proxyBase) {
      return `${this.proxyBase}/api/video/proxy?baseUrl=${encodeURIComponent(this.baseUrl)}&path=${encodeURIComponent(path)}&${query}`;
    }
    return `${this.baseUrl}${path}?${query}`;
  }

  private buildStreamUrl(videoUrl: string): string {
    if (this.proxyBase) {
      return `${this.proxyBase}/api/video/stream?url=${encodeURIComponent(videoUrl)}`;
    }
    return videoUrl;
  }
}

// Factory
export class MacCMSSourceFactory implements MediaSourceFactory {
  readonly factoryId = MACCMS_FACTORY_ID;
  readonly allowMultipleInstances = true;
  readonly parameters: SourceParameter[] = [
    { name: 'baseUrl', displayName: '源地址', type: 'string', required: true },
    { name: 'name', displayName: '源名称', type: 'string', required: true },
  ];
  readonly info: MediaSourceInfo = {
    displayName: '苹果CMS源',
    description: '支持苹果CMS V10 API 标准的视频源',
  };

  create(sourceId: string, config: SourceConfig): MediaSource {
    const baseUrl = config.arguments.baseUrl || '';
    const name = config.arguments.name || '未命名源';
    return new MacCMSSource(sourceId, name, baseUrl);
  }
}

// 预设源配置
export const DEFAULT_MACCMS_SOURCES = [
  { sourceId: 'kuapi', name: '酷云资源', baseUrl: 'https://kuapi.co' },
  { sourceId: 'bfzy', name: '暴风资源', baseUrl: 'https://bfzyapi.com' },
  { sourceId: 'guangsu', name: '光速资源', baseUrl: 'https://guangsuapi.com' },
  { sourceId: 'sdzy', name: '闪电资源', baseUrl: 'https://sdzyapi.com' },
];
```

- [ ] **Step 2: Commit**

```bash
git add src/services/media/sources/MacCMSSource.ts
git commit -m "feat(video-v2): add MacCMSSource with factory and default sources"
```

---

### Task 5: MediaSelector

**Files:**
- Create: `src/services/media/MediaSelector.ts`

- [ ] **Step 1: 创建 MediaSelector.ts**

```typescript
// src/services/media/MediaSelector.ts
import { MediaMatch, MatchKind, MediaFetchRequest } from './types';
import { MatchEngine } from './MatchEngine';

export class MediaSelector {
  /**
   * 从多个源的结果中选出最佳资源
   * 策略：EXACT 优先 → tier 排序 → 第一个
   */
  static selectBest(matches: MediaMatch[]): MediaMatch | null {
    if (matches.length === 0) return null;
    const sorted = MatchEngine.sortMatches(matches);
    return sorted[0];
  }

  /**
   * 按源分组
   */
  static groupBySource(matches: MediaMatch[]): Map<string, MediaMatch[]> {
    const groups = new Map<string, MediaMatch[]>();
    for (const match of matches) {
      const sourceId = match.media.sourceId;
      if (!groups.has(sourceId)) groups.set(sourceId, []);
      groups.get(sourceId)!.push(match);
    }
    return groups;
  }

  /**
   * 过滤只保留指定匹配度的结果
   */
  static filterByMatchKind(matches: MediaMatch[], kind: MatchKind): MediaMatch[] {
    return matches.filter(m => m.matchKind === kind);
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/services/media/MediaSelector.ts
git commit -m "feat(video-v2): add MediaSelector for best-match selection"
```

---

### Task 6: 初始化注册默认源

**Files:**
- Create: `src/services/media/initSources.ts`

- [ ] **Step 1: 创建 initSources.ts**

```typescript
// src/services/media/initSources.ts
import { mediaSourceManager } from './MediaSourceManager';
import { MacCMSSourceFactory, DEFAULT_MACCMS_SOURCES } from './sources/MacCMSSource';

let initialized = false;

export function initMediaSources(): void {
  if (initialized) return;
  initialized = true;

  // 注册工厂
  mediaSourceManager.registerFactory(new MacCMSSourceFactory());

  // 注册默认源（如果尚未注册）
  const existing = mediaSourceManager.getRegistrations();
  const existingIds = new Set(existing.map(r => r.sourceId));

  for (const source of DEFAULT_MACCMS_SOURCES) {
    if (!existingIds.has(source.sourceId)) {
      mediaSourceManager.addRegistration({
        sourceId: source.sourceId,
        factoryId: 'maccms',
        config: {
          arguments: {
            baseUrl: source.baseUrl,
            name: source.name,
          },
        },
        enabled: true,
      });
    }
  }
}
```

- [ ] **Step 2: 在 App.jsx 中调用初始化**

在 `src/App.jsx` 的顶层，import 并调用 `initMediaSources()`。

- [ ] **Step 3: Commit**

```bash
git add src/services/media/initSources.ts src/App.jsx
git commit -m "feat(video-v2): add source initialization with default MacCMS sources"
```

---

### Task 7: VideoHome 页面

**Files:**
- Create: `src/components/Video/VideoHome.jsx`
- Create: `src/components/Video/VideoHome.css`

- [ ] **Step 1: 创建 VideoHome.jsx — Bangumi 条目搜索首页**

核心功能：
- 搜索栏调 Bangumi API 搜索条目
- 展示条目卡片（封面、标题、评分、类型标签）
- 点击条目跳转 `/video/subject/:subjectId`
- 底部展示 Bangumi 热门排行 + 我的追番

使用已有的 `BangumiService` 搜索接口。搜索结果用 `SubjectCard` 组件展示。

- [ ] **Step 2: 创建 VideoHome.css**

沿用项目统一的 UI 风格：pill-shaped tags、pink primary、hover lift、rounded cards。

- [ ] **Step 3: Commit**

```bash
git add src/components/Video/VideoHome.jsx src/components/Video/VideoHome.css
git commit -m "feat(video-v2): add VideoHome with Bangumi subject search"
```

---

### Task 8: SubjectDetail 页面

**Files:**
- Create: `src/components/Video/SubjectDetail.jsx`
- Create: `src/components/Video/SubjectDetail.css`
- Create: `src/components/Video/MediaMatchList.jsx`
- Create: `src/components/Video/MediaMatchList.css`

- [ ] **Step 1: 创建 SubjectDetail.jsx — 番剧详情页**

核心功能：
- 路由 `/video/subject/:subjectId`
- 调 Bangumi API 获取条目详情（封面、标题、评分、简介、标签）
- 调 Bangumi API 获取剧集列表
- 点击某集 → 构建 MediaFetchRequest → 调 mediaSourceManager.fetchAll → 展示 MediaMatchList
- 点击资源 → 跳转 `/video/play/:subjectId/:episodeId?sourceId=&mediaId=`

- [ ] **Step 2: 创建 MediaMatchList.jsx — 资源匹配列表**

展示每个 MediaMatch：
- 源名称 + 匹配度标签（精确=绿色/模糊=黄色）
- 分辨率、字幕组、大小（从 properties 读取）
- 点击跳转播放页

- [ ] **Step 3: 创建 CSS 文件**

- [ ] **Step 4: Commit**

```bash
git add src/components/Video/SubjectDetail.jsx src/components/Video/SubjectDetail.css src/components/Video/MediaMatchList.jsx src/components/Video/MediaMatchList.css
git commit -m "feat(video-v2): add SubjectDetail and MediaMatchList components"
```

---

### Task 9: VideoPlayer 页面

**Files:**
- Create: `src/components/Video/VideoPlayer.jsx`
- Create: `src/components/Video/VideoPlayer.css`

- [ ] **Step 1: 创建 VideoPlayer.jsx — 播放器页**

核心功能：
- 路由 `/video/play/:subjectId/:episodeId`
- 从 URL query params 获取 sourceId 和 mediaId
- 通过 mediaSourceManager 获取源实例
- 重新 fetch 获取 Media 对象
- DPlayer 初始化播放（复用现有 DPlayer + Hls.js 逻辑）
- 播放错误处理（复用之前的 playError 状态）
- 播放进度记忆（复用之前的 localStorage 逻辑）
- 剧集快速切换列表
- 资源切换（不同源的同一集）

- [ ] **Step 2: 创建 VideoPlayer.css**

- [ ] **Step 3: Commit**

```bash
git add src/components/Video/VideoPlayer.jsx src/components/Video/VideoPlayer.css
git commit -m "feat(video-v2): add VideoPlayer with DPlayer and progress memory"
```

---

### Task 10: SourceManager 页面

**Files:**
- Create: `src/components/Video/SourceManager.jsx`
- Create: `src/components/Video/SourceManager.css`

- [ ] **Step 1: 创建 SourceManager.jsx — 源管理页**

核心功能：
- 路由 `/video/sources`
- 展示所有已注册源（从 mediaSourceManager.getRegistrations() 读取）
- 每个源：启用/禁用开关、测试连接按钮
- 添加新源：选择工厂类型 → 填写参数 → 创建
- 删除自定义源

- [ ] **Step 2: 创建 SourceManager.css**

- [ ] **Step 3: Commit**

```bash
git add src/components/Video/SourceManager.jsx src/components/Video/SourceManager.css
git commit -m "feat(video-v2): add SourceManager for source configuration"
```

---

### Task 11: 路由更新与旧文件清理

**Files:**
- Modify: `src/App.jsx` — 更新路由
- Delete: `src/components/Video/VideoZone.jsx`
- Delete: `src/components/Video/VideoZone.css`
- Delete: `src/components/Video/VideoDetail.jsx`
- Delete: `src/components/Video/VideoDetail.css`
- Delete: `src/services/videoSource.js`

- [ ] **Step 1: 更新 App.jsx 路由**

将 `/video` 路由指向 `VideoHome`，添加新路由：
- `/video/subject/:subjectId` → SubjectDetail
- `/video/play/:subjectId/:episodeId` → VideoPlayer
- `/video/sources` → SourceManager

- [ ] **Step 2: 删除旧文件**

- [ ] **Step 3: 验证应用可正常启动**

Run: `cd d:\Desktop\Ideas\ANISpace\ANISpace && npm run dev`

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat(video-v2): update routes and remove legacy video components"
```

---

### Task 12: 端到端验证

- [ ] **Step 1: 启动开发服务器**

Run: `cd d:\Desktop\Ideas\ANISpace\ANISpace && npm run dev`

- [ ] **Step 2: 验证搜索流程**

1. 打开影视首页
2. 搜索"芙莉莲" → 应显示 Bangumi 条目列表
3. 点击条目 → 应显示番剧详情页（封面、评分、剧集列表）
4. 点击某集 → 应显示资源匹配列表
5. 点击资源 → 应跳转播放页并开始播放

- [ ] **Step 3: 验证源管理**

1. 打开源管理页
2. 禁用某源 → 搜索时该源不参与查询
3. 添加自定义源 → 搜索时可查询该源
4. 测试连接 → 显示连接状态

- [ ] **Step 4: 修复发现的问题**

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "feat(video-v2): P0 complete - core layer + MacCMS + UI rewrite"
```
